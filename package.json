{
  "name": "hood-hope-website",
  "version": "1.0.0",
  "private": true,
  "description": "H.O.O.D HOPE production website — dependency-free static site generator.",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && npx --yes serve . -l 4000"
  }
}
