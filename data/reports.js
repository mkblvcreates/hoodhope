// Reports & financial transparency data — loaded live from
// content/reports.json, the file the CMS (/admin/ → "Reports") edits. The
// St. Louis Flagship pilot's budget allocation and ROI projections live in
// content/model.json and are rendered on both /reports/ and /model/.

module.exports = require("../content/reports.json");
